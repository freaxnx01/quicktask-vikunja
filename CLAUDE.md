# CLAUDE.md

Instructions for Claude (and other coding agents) working in this repo.

## What this app is

QuickTask is a Flutter app whose primary purpose is to act as an **Android share target**: the user shares a URL, text, or file from another app and QuickTask creates a task in a self-hosted Vikunja instance. Desktop (Windows/Linux) is supported for manual entry only — share-intent flows are mobile-only.

## Queued work

Open items that aren't yet issues live in `TODO.md` at the repo root. Check it at the start of any session where the user hasn't given you a specific task — pick the top unchecked item, implement it, open a PR, and check the box in the same PR.

## Architecture (one-paragraph)

`lib/main.dart` owns the share-intent listener (`receive_sharing_intent`) and a tiny route stack: `RecentTasksScreen` (home) → `ProjectPickerScreen` (push) → `TaskConfirmationScreen` (pushReplacement). Data layer in `lib/data/` (Vikunja API client, secure storage for token, local task history & project-usage tracker via `shared_preferences`, HTML title fetcher). Models in `lib/models/`.

## Commands

```bash
flutter pub get               # install deps
flutter analyze               # lint - run before claiming work is done
flutter test                  # widget + unit tests - run before claiming work is done
flutter run -d linux          # fast iteration on desktop (no share intents)
make apk                      # release APK -> build/app/outputs/flutter-apk/
make push PHONE_IP=192.168.x.y  # send APK to phone via LocalSend
```

**Always run `flutter analyze` and `flutter test` before opening a PR.** Do not skip — the CI/automation expects clean output.

## Conventions

- **Branch naming for autonomous work:** `claude/issue-<N>-<short-slug>`
- **PR body:** must include `Closes #<N>` so the issue auto-closes on merge
- **Don't touch:** `lib/build_info.dart` (generated by `tool/build.sh`)
- **Stay scoped:** fix the issue, no incidental refactors. If you spot something else worth doing, mention it in the PR body, don't bundle it.
- **No new dependencies** without explicit approval — keep `pubspec.yaml` minimal.

## Testing — read this before writing code

The current `test/widget_test.dart` is a leftover stub from `flutter create` and references a non-existent `MyApp` — it does not run. Treat it as "no real tests exist yet."

**Most bugs in this app are testable without a device:**

- Navigator/route bugs (the dominant class of bug here) → widget tests with `WidgetTester`
- Share-intent handling → mock `ReceiveSharingIntent` and pump frames
- Vikunja API logic → unit tests against an HTTP mock
- Title fetching → unit tests against canned HTML

**Before fixing a bug, write a failing widget/unit test that reproduces it.** Manual phone testing is slow and unreliable as a primary feedback loop — reserve it for genuinely device-only concerns (real share-sheet entry, file-system attachments, OEM quirks).

## Things to be careful about

- **Share-intent activity lifecycle is the #1 bug source.** `MainActivity` is `singleTop` with `taskAffinity=""`. The route stack persists across multiple shares unless explicitly cleared. When in doubt, clear back to `RecentTasksScreen` between shares.
- **`getInitialMedia()` must be `reset()`'d** after handling, or it can replay on rebuild.
- **Mobile vs desktop:** wrap mobile-only code in `if (_isMobile)` (see `lib/main.dart`). Don't assume `receive_sharing_intent` works on desktop.
- **Vikunja token is in secure storage** — never log it, never write it to disk in plaintext, never include it in error messages or PR descriptions.
- **No telemetry, no analytics, no crash reporting** — this app is intentionally minimal and runs against a personal self-hosted instance.

## When you don't understand something

- **Ask in an issue comment** instead of guessing. The autonomous trigger explicitly allows this: comment with the clarifying question, remove the `claude-working` label, exit. A human will re-label `claude-ready` once they answer.
- **Don't invent product/UX decisions.** Things like "should the picker auto-select the most recent project?" are product calls, not implementation choices.

## What lives where

| Path | Purpose |
|---|---|
| `lib/main.dart` | App root, share listener, route orchestration |
| `lib/ui/` | Screens (setup, recent tasks, picker, confirmation) |
| `lib/data/` | API client, storage, history, title fetcher |
| `lib/models/` | Data classes (Project, VikunjaTask, SharedContent) |
| `tool/build.sh` | Builds APK / Windows, regenerates `build_info.dart` |
| `tool/push-to-phone.sh` | LocalSend deploy helper |
| `docs/superpowers/{specs,plans}/` | Design docs / implementation plans (workflow convention) |
| `android/app/src/main/AndroidManifest.xml` | Share intent filters live here |
